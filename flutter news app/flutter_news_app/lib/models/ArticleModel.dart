class ArticleModel{
  String authorName;
  String title;
  String desc;
  String url;
  String urlToImage;
  String content;
  String publishedAt;

  ArticleModel({this.authorName, this.title, this.desc, this.url,
      this.urlToImage, this.content, this.publishedAt});
}